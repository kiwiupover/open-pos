<h1>Editing <small>{{nameAndPrice}}</small></h1>

<form {{action save on='submit'}}>
  {{input valueBinding="name" type='text'}}
  {{textarea valueBinding="description"}}
  {{input valueBinding="priceCents" type="text"}}
  {{input valueBinding="nameAndPrice" type="text"}}

  {{input type="submit" value="submit" class="button"}}
</form>
