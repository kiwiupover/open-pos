<ul class="no-bullet">
	{{#each category in content}}
		{{#view Pos.CategoryItemView contentBinding="category"}}
			<a href="#" {{action changeView category.id}}>
				<li>{{category.name}}</li>
			</a>
		{{/view}}
	{{/each}}
</ul>